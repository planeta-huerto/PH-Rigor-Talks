#!/usr/bin/env php
<?php
require dirname(__DIR__) . '/vendor/autoload.php';

use PH\Temperature\Application\Command\CheckTemperatureCommand;
use Symfony\Component\Console\Application;

$app = new Application();
$app->add(new CheckTemperatureCommand());
$app->run();